---
// src/components/ui/NavLink.astro
import Icon from '@/components/ui/icons/Icon.astro';

interface Props {
  href: string;
  current?: boolean;
  target?: string;
  rel?: string;
  class?: string;
  isExternal?: boolean;
}

const {
  href,
  current = false,
  target,
  rel,
  class: className,
  isExternal = false,
} = Astro.props;

const ariaCurrentValue = ("Halaman") as "page";
---

<a
  href={href}
  target={target}
  rel={rel}
  aria-current={current ? ariaCurrentValue : undefined}
  class:list={[
    "relative inline-flex items-center gap-1 px-3 py-1.5 text-label-14 font-medium rounded-full transition-all duration-200",
    current
      ? "text-(--accent) bg-none"
      : "text-muted hover:text-strong hover:bg-subtle",
    className,
  ]}
>
  <slot />
  {isExternal && (
    <Icon
      name="external"
      className="w-3 h-3 opacity-40 group-hover:opacity-100 transition-opacity duration-200"
    />
  )}
</a>