---
// src/components/ui/Breadcrumb.astro
interface Props {
  items: Array<{ name: string; href?: string }>;
}

const { items = [] } = Astro.props;
if (!items.length) return;
---

<nav aria-label="Breadcrumb" class="flex justify-center pb-8">
  <ol class="flex flex-wrap items-center gap-x-2 text-[13px] text-(--fg-muted)">
    {items.map((item, index) => (
      <li class="flex items-center gap-x-2">
        {index > 0 && (
          <span aria-hidden="true" class="text-(--fg-subtle)">
            /
          </span>
        )}
        {index === items.length - 1? (
          <span aria-current="page" class="text-(--fg-muted)">
            {item.name}
          </span>
        ) : (
          <a
            href={item.href}
            class="hover:text-(--fg)"
          >
            {item.name}
          </a>
        )}
      </li>
    ))}
  </ol>
</nav>