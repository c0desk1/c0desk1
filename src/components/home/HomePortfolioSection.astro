---
// src/components/home/HomePortfolioSection.astro

import Container from '../ui/Container.astro';
import ProjectCard from '@/components/portfolio/ProjectCard.astro';

interface Props {
  projects: any[];
}

const { projects } = Astro.props;
---

<section class="border-b border-zinc-100 dark:border-zinc-900">
  <Container size="md" class="py-10 md:py-12">

    <div class="flex items-center justify-between mb-5">
      <div class="flex items-center gap-2">
        <div class="h-1.5 w-1.5 rounded-full bg-emerald-500"></div>
        <h2 class="text-sm text-zinc-500 dark:text-zinc-400">
          Portfolio
        </h2>
      </div>

      <a
        href="/portfolio"
        class="group flex items-center gap-1 text-sm text-zinc-500 hover:text-zinc-900 dark:hover:text-white transition-colors"
      >
        <span>Lihat semua</span>
        <span class="group-hover:translate-x-0.5 transition-transform">→</span>
      </a>
    </div>

    {projects.length > 0 ? (
      <div class="space-y-0 divide-y divide-zinc-100 dark:divide-zinc-900">
        {projects.map((project) => (
          <ProjectCard project={project} />
        ))}
      </div>
    ) : (
      <div class="text-center py-8">
        <p class="text-zinc-500 dark:text-zinc-400 text-sm">
          ▼ Belum ada proyek.
        </p>
      </div>
    )}

  </Container>
</section>