---
import Container from '../ui/Container.astro';

interface Props {
  src: any; 
  alt?: string;
  caption?: string;
}

const { src, alt, caption } = Astro.props;
const finalSrc = typeof src === 'object' ? src.src : src;

const finalCaption = caption || alt;
---

<div class="border-b border-zinc-100 dark:border-zinc-900">
  <Container size="md" class="py-6 md:py-8">
    <figure class="overflow-hidden border border-zinc-200 dark:border-zinc-800 bg-zinc-100 dark:bg-zinc-900">
      <div class="relative aspect-video overflow-hidden">
        <img 
          src={finalSrc}
          alt={alt || 'Project image'}
          class="absolute inset-0 w-full h-full object-cover"
          loading="eager"
          decoding="async"
          sizes="(min-width: 768px) 768px, 100vw"
        />
      </div>
      {finalCaption && (
        <figcaption class="px-2 py-1.5 border-t border-zinc-200 dark:border-zinc-800 text-xs text-zinc-500 dark:text-zinc-400">
          <span class="text-zinc-400 dark:text-zinc-600 mr-1">
            //
          </span>
          {finalCaption}
        </figcaption>
      )}
    </figure>
  </Container>
</div>
