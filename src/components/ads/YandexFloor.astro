<script is:inline>
  (() => {
    const isMobile = window.matchMedia("(max-width: 768px)").matches;

    if (!isMobile) return;

    const shouldShowAd = (() => {
      window.__adPageCount ??= 0;
      window.__lastFloorPage ??= 0;

      window.__adPageCount++;

      const current = window.__adPageCount;
      const gap = Math.floor(Math.random() * 3) + 2;

      if (current - window.__lastFloorPage < gap) {
        return false;
      }

      window.__lastFloorPage = current;
      return true;
    })();

    if (!shouldShowAd) return;

    window.yaContextCb ||= [];

    window.addEventListener(
      "load",
      () => {
        window.yaContextCb.push(() => {
          Ya.Context.AdvManager.render({
            blockId: "R-A-19179231-1",
            type: "floorAd",
            platform: "touch",
          });
        });
      },
      { once: true }
    );
  })();
</script>
