---
// src/components/blog/BlogRelatedPosts.astro

import type { CollectionEntry } from 'astro:content';
import RelatedPostCard from './RelatedPostCard.astro';

interface Props {
  posts: CollectionEntry<'blog'>[];
}

const { posts } = Astro.props;

---

{posts.length > 0 && (
  <div class="section-divider py-4 md:py-6">
    <div class="flex items-center mb-3 md:mb-6">
      <h2 class="text-lg md:text-xl font-medium text-(--fg)">
        Baca juga.
      </h2>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-3 sm:gap-x-0 sm:gap-y-4 md:gap-y-6 md:gap-y-4 divide-y md:divide-y-0 divide-(--border)">
      {posts.map(post => (
        <RelatedPostCard post={post} />
      ))}
    </div>
  </div>
)}