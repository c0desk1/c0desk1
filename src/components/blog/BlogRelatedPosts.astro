---
// src/components/blog/BlogRelatedPosts.astro
import type { CollectionEntry } from 'astro:content';
import RelatedPostCard from './RelatedPostCard.astro';

interface Props {
  posts: CollectionEntry<'blog'>[];
}

const { posts } = Astro.props;
---

{posts.length > 0 && (
  <section class="mt-16 pt-8 border-t border-zinc-100 dark:border-zinc-900">
    <div class="flex items-center gap-3 mb-6">
      <div class="h-px w-8 bg-linear-to-r from-zinc-300 to-transparent dark:from-zinc-700"></div>
      <h3 class="text-xs md:text-sm font-mono font-bold uppercase tracking-wider text-zinc-500 dark:text-zinc-400">
        Further reading
      </h3>
      <div class="h-px flex-1 bg-linear-to-l from-zinc-100 to-transparent dark:from-zinc-900"></div>
    </div>
    
    <div class="space-y-1">
      {posts.map(post => (
        <RelatedPostCard 
          post={post}
          showDate={true}
          showReadingTime={true}
        />
      ))}
    </div>
  </section>
)}