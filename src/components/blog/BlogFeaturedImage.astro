---
// src/components/blog/BlogFeaturedImage.astro

interface Props {
  image: string;
  alt?: string;
  caption?: string;
}

const { image, alt, caption } = Astro.props;

const finalCaption = caption || alt;
---

<div class="prose-custom mx-auto max-w-3xl my-4 md:my-8">
  <figure class="overflow-hidden">
    <div class="relative aspect-video overflow-hidden">
      <div
        class="absolute inset-0 animate-pulse bg-zinc-100 dark:bg-zinc-900"
        aria-hidden="true"
      ></div>
      <img
        src={image}
        alt={alt || caption}
        class="relative z-10 w-full h-full object-cover opacity-0 transition-opacity duration-500"
        loading="eager"
        decoding="async"
        onload="this.style.opacity='1'; this.previousElementSibling.style.display='none';"
      />
    </div>
    {finalCaption && (
      <figcaption class="text-xs md:text-[13px] text-center leading-snug">
        {finalCaption}
      </figcaption>
    )}
  </figure>
</div>