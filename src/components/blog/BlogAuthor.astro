---
// src/components/blog/BlogAuthor.astro

import { siteConfig } from '@/config/site';

interface Props {
  name?: string;
  avatar?: string;
  title?: string;
}

const { name, avatar, title } = Astro.props;

const authorName = name || siteConfig.author;
const authorAvatar = avatar || siteConfig.authorAvatar;
const authorTitle = title || siteConfig.authorTitle;
---

<div class="flex justify-center items-center gap-3 text-xs md:text-sm" aria-label={`Author: ${authorName}`}>
  {authorAvatar && (
    <img
      src={authorAvatar}
      alt={`Avatar of ${authorName}`}
      class="w-6 h-6 rounded-full object-cover opacity-90"
      loading="lazy"
    />
  )}
  <div class="flex gap-2 leading-tight">
    <div class="text-zinc-900 dark:text-zinc-100">
      {authorName}
    </div>
    {authorTitle && (
      <div class="text-zinc-500 dark:text-zinc-400">
        {authorTitle}
      </div>
    )}
  </div>
</div>