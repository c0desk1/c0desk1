---
// src/components/blog/BlogTOC.astro

import { filterHeadingsForTOC } from '@/lib/utils';

interface Heading {
  depth: number;
  slug: string;
  text: string;
}

interface Props {
  headings: Heading[];
}

const { headings } = Astro.props;
const tocHeadings = filterHeadingsForTOC(headings);
---

{tocHeadings.length > 0 && (
  <nav class="not-prose my-4 md:my-8 rounded-xl border border-black/10 dark:border-white/10 bg-zinc-50/50 dark:bg-zinc-900/30 p-5 md:p-7">
    <h2 class="mb-4 text-[11px] font-semibold uppercase tracking-widest text-zinc-500 dark:text-zinc-400">
      Daftar Isi
    </h2>
    <ul class="space-y-2">
      {tocHeadings.map(h => (
        <li
          class:list={[
            "text-[13px] leading-snug",
            h.depth === 3
              ? "pl-4"
              : ""
          ]}
        >
          <a
            href={`#${h.slug}`}
            class="group flex items-start gap-2 text-zinc-600 dark:text-zinc-400 hover:text-zinc-900 dark:hover:text-zinc-100 transition-colors duration-150"
          >
            <span class="mt-[6px] h-[3px] w-[3px] shrink-0 rounded-full bg-zinc-400 dark:bg-zinc-600"></span>
            <span class="leading-snug">
              {h.text}
            </span>
          </a>
        </li>
      ))}
    </ul>
  </nav>
)}